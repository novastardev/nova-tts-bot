{
  "name": "nova-tts-bot",
  "type": "python",
  "region": "oregon",
  "buildCommand": "pip install -r requirements.txt",
  "startCommand": "python3 bot.py",
  "healthCheckPath": "/health",
  "initialSize": "basic",
  "instances": 1,
  "restartPolicy": {
    "name": "on_failure",
    "maxRetries": 5
  }
}
